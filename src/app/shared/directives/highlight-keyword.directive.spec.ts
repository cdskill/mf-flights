import { HighlightKeywordDirective } from './highlight-keyword.directive';

describe('HighlightKeywordDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightKeywordDirective();
    expect(directive).toBeTruthy();
  });
});
